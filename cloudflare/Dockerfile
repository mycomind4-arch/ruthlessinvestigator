FROM docker.io/cloudflare/sandbox:0.10.3

# Keep the Director's computer intentionally small. Add research-specific
# packages through approved skills rather than baking arbitrary tools into the image.
WORKDIR /workspace
